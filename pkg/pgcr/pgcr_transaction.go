package pgcr

import (
	"database/sql"

	"rivenbot/types/entity"
)

// Saves a processed pgcr and its fields to its respective tables
func Save(processedPgcr *entity.ProcessedPostGameCarnageReport, db *sql.DB) {
  
}
